{
  "name": "kaiku",
  "version": "1.7.0",
  "description": "Lightweight UI framework",
  "license": "MIT",
  "author": "Teemu Pääkkönen",
  "main": "dist/kaiku.js",
  "types": "dist/kaiku.d.ts",
  "files": [
    "dist/*"
  ],
  "scripts": {
    "test": "jest",
    "build:dev": "NODE_ENV=development rm -rf dist/ && node build.js",
    "build": "rm -rf dist/ && NODE_ENV=production node build.js",
    "prettier": "prettier --write .",
    "prepublish": "yarn prettier && yarn build"
  },
  "dependencies": {},
  "devDependencies": {
    "@babel/plugin-transform-async-to-generator": "^7.14.5",
    "@babel/plugin-transform-react-jsx": "^7.14.5",
    "@babel/preset-env": "^7.14.7",
    "esbuild": "^0.12.15",
    "jest": "^27.0.6",
    "prettier": "^2.3.2",
    "regenerator-runtime": "^0.13.7",
    "typescript": "^4.3.5"
  }
}
